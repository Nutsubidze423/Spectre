// Phase 5: generates PNG thumbnail from canvas element JSON for board preview.
export class CanvasSnapshotService {
  async generateThumbnail(_elementsJson: unknown): Promise<string> {
    // Phase 5: use node-canvas or sharp to render a 400×300 thumbnail
    throw new Error('Phase 5 — not yet implemented');
  }
}
