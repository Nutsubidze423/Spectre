import type { Server, Socket } from 'socket.io';

// Phase 3: handles all room:*, cursor:*, stroke:*, element:* socket events.
export function registerRoomHandler(_io: Server, _socket: Socket): void {
  // Phase 3 implementation
}
